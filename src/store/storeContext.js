/* import {createContext } from 'react'
export const Context = createContext(null)
const UserProvider = ({children}) => {
return (
    <Context.Provider value ={5}>
        {children}
    </Context.Provider>
)
}
export default UserProvider */ 


///Dejo este codigo unicamente para guiarse con la sintaxis del appContext, despues hay que adaptarlo a la logica de nuestro programa.